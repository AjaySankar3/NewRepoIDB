import { CustomeStylrDirective } from './custome-stylr.directive';

describe('CustomeStylrDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomeStylrDirective();
    expect(directive).toBeTruthy();
  });
});
